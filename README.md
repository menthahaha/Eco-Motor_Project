# ЭКО-МОТОР - Сезонное хранение колес

Веб-приложение для онлайн-бронирования ячеек хранения колес автосервиса ЭКО-МОТОР в Пересвете.

## 🚀 Возможности

- **Онлайн-бронирование ячеек** - выбор и бронирование ячеек с указанием периода хранения
- **Личный кабинет** - управление бронированиями, просмотр истории
- **Система отзывов** - пользователи могут оставлять отзывы (с модерацией)
- **Административная панель** - управление бронированиями, пользователями, отзывами и контактами
- **Адаптивный дизайн** - корректное отображение на всех устройствах
- **Темная тема** - поддержка светлой и темной темы оформления
- **Калькулятор цен** - автоматический расчет стоимости хранения в зависимости от периода

## 📋 Технологии

- **React 18** - для построения пользовательского интерфейса
- **Tailwind CSS** - для стилизации компонентов
- **Babel Standalone** - для трансформации JSX в браузере
- **Yandex Maps API** - для отображения карты
- **Lucide Icons** - иконки
- **JSON Database** - хранение данных в `database.json`

## 📁 Структура проекта

```
Eco-Motor_Project/
├── index.html          # Главная страница
├── about.html          # Страница "О компании"
├── cabinet.html        # Личный кабинет
├── contacts.html       # Контакты
├── reviews.html        # Отзывы
├── services.html       # Услуги
├── faq.html            # FAQ
├── privacy.html        # Политика конфиденциальности
├── database.json       # База данных (пользователи, бронирования, отзывы)
├── css/
│   └── styles.css      # Глобальные стили и темы
├── js/
│   ├── script.js       # Главный скрипт роутинга
│   └── modules/
│       ├── components/ # React компоненты (Header, Footer, Hero, etc.)
│       ├── pages/      # Страницы приложения
│       └── utils/      # Утилиты (storage, validation, theme)
└── images/             # Изображения
```

## 🔧 Установка и запуск

### Локальный запуск

1. Клонируйте репозиторий:
```bash
git clone https://github.com/your-username/Eco-Motor_Project.git
cd Eco-Motor_Project
```

2. Запустите локальный сервер (например, через Python или Node.js):

**Python 3:**
```bash
python -m http.server 8000
```

**Python 2:**
```bash
python -m SimpleHTTPServer 8000
```

**Node.js (с http-server):**
```bash
npx http-server -p 8000
```

3. Откройте в браузере: `http://localhost:8000`

### Размещение на GitHub Pages

1. Создайте репозиторий на GitHub
2. Загрузите все файлы проекта в репозиторий
3. Перейдите в **Settings** → **Pages**
4. В разделе **Source** выберите ветку `main` (или `master`) и папку `/ (root)`
5. Нажмите **Save**
6. Ваш сайт будет доступен по адресу: `https://your-username.github.io/Eco-Motor_Project/`

> **Важно:** Файл `.nojekyll` уже включен в проект, чтобы GitHub Pages не обрабатывал файлы через Jekyll.

## 👤 Доступы

### Администратор
- **Email:** `admin@ya.ru`
- **Пароль:** `12345Q!!`

### Тестовые пользователи
Пользователи хранятся в `database.json`. При регистрации новые пользователи сохраняются в localStorage и автоматически скачивается обновленный `database.json` для ручной замены на сервере.

## 📝 Особенности работы с данными

Проект использует гибридный подход к хранению данных:
- **database.json** - основная база данных на сервере
- **localStorage** - кэш для быстрого доступа и новых записей

При регистрации нового пользователя:
1. Данные сохраняются в localStorage
2. Автоматически скачивается обновленный `database.json`
3. Администратор должен вручную заменить файл на сервере

## 🎨 Темы оформления

Приложение поддерживает светлую и темную темы. Переключение темы доступно через кнопку в шапке сайта. Настройки темы сохраняются в localStorage.

## 📱 Адаптивность

Сайт полностью адаптирован для:
- Десктопов (1920px+)
- Планшетов (768px - 1024px)
- Мобильных устройств (320px - 767px)

## 🔐 Безопасность

- Валидация всех форм на клиенте
- Хеширование паролей (можно добавить на бэкенде)
- Проверка прав доступа для администратора
- Санитизация пользовательского ввода

## 📄 Лицензия

Этот проект создан в учебных целях.

## 👨‍💻 Разработка

Для разработки рекомендуется использовать локальный сервер, так как некоторые функции (загрузка `database.json`) требуют HTTP-протокола и не работают при открытии файлов напрямую через `file://`.

## 🐛 Известные ограничения

- Из-за ограничений безопасности браузера, клиентский JavaScript не может напрямую записывать в файлы на сервере. Поэтому обновления `database.json` требуют ручной замены файла администратором.
- В продакшене рекомендуется использовать полноценный бэкенд с API для работы с базой данных.

## 📞 Контакты

ООО ЭКО-МОТОР  
Пересвет, Московская область

---

**Версия:** 1.0.0  
**Последнее обновление:** 2025

