# Инструкция по развертыванию на GitHub Pages

## Шаг 1: Создание репозитория на GitHub

1. Войдите в свой аккаунт GitHub
2. Нажмите кнопку **"New"** (или **"+"** → **"New repository"**)
3. Заполните форму:
   - **Repository name:** `Eco-Motor_Project` (или другое имя)
   - **Description:** "Веб-приложение для онлайн-бронирования ячеек хранения колес"
   - Выберите **Public** (или Private, если нужен приватный репозиторий)
   - **НЕ** добавляйте README, .gitignore или лицензию (они уже есть в проекте)
4. Нажмите **"Create repository"**

## Шаг 2: Загрузка файлов в репозиторий

### Вариант A: Через командную строку (Git)

1. Откройте терминал в папке проекта
2. Инициализируйте Git (если еще не сделано):
```bash
git init
```

3. Добавьте все файлы:
```bash
git add .
```

4. Создайте первый коммит:
```bash
git commit -m "Initial commit: Eco-Motor Project"
```

5. Добавьте удаленный репозиторий (замените `your-username` на ваш GitHub username):
```bash
git remote add origin https://github.com/your-username/Eco-Motor_Project.git
```

6. Загрузите файлы:
```bash
git branch -M main
git push -u origin main
```

### Вариант B: Через веб-интерфейс GitHub

1. На странице созданного репозитория нажмите **"uploading an existing file"**
2. Перетащите все файлы проекта в окно браузера
3. Прокрутите вниз, введите сообщение коммита: "Initial commit"
4. Нажмите **"Commit changes"**

## Шаг 3: Настройка GitHub Pages

1. В репозитории перейдите в **Settings** (настройки)
2. В левом меню найдите раздел **Pages**
3. В разделе **Source**:
   - Выберите ветку: **main** (или **master**)
   - Выберите папку: **/ (root)**
4. Нажмите **Save**

## Шаг 4: Ожидание публикации

- GitHub Pages обычно публикует сайт в течение 1-2 минут
- После сохранения настроек появится сообщение: *"Your site is live at https://your-username.github.io/Eco-Motor_Project/"*
- Если сообщение не появилось сразу, подождите несколько минут и обновите страницу

## Шаг 5: Проверка работы сайта

1. Откройте ссылку вашего сайта: `https://your-username.github.io/Eco-Motor_Project/`
2. Проверьте основные страницы:
   - Главная страница
   - Личный кабинет (вход/регистрация)
   - Отзывы
   - Контакты
3. Проверьте работу администратора:
   - Войдите с `admin@ya.ru` / `12345Q!!`
   - Убедитесь, что открывается админ-панель

## ⚠️ Важные замечания

### Работа с database.json

- Файл `database.json` будет доступен для чтения через GitHub Pages
- При регистрации новых пользователей автоматически скачивается обновленный `database.json`
- Для обновления базы данных на GitHub Pages:
  1. Скачайте обновленный `database.json` после регистрации пользователя
  2. Загрузите его обратно в репозиторий (через веб-интерфейс или Git)
  3. Сделайте коммит и пуш изменений

### Обновление сайта

После любых изменений в коде:
```bash
git add .
git commit -m "Описание изменений"
git push
```

Изменения появятся на GitHub Pages в течение 1-2 минут.

### Кастомный домен (опционально)

Если у вас есть собственный домен:
1. В настройках GitHub Pages добавьте домен в поле **Custom domain**
2. Настройте DNS записи у вашего регистратора домена:
   - Тип: `CNAME`
   - Имя: `www` (или `@` для корневого домена)
   - Значение: `your-username.github.io`

## 🔧 Решение проблем

### Сайт не загружается
- Проверьте, что файл `index.html` находится в корне репозитория
- Убедитесь, что файл `.nojekyll` присутствует в корне
- Проверьте настройки Pages в Settings → Pages

### Изображения не отображаются
- Убедитесь, что все изображения находятся в папке `images/`
- Проверьте, что пути к изображениям относительные (начинаются с `images/`, а не `/images/`)

### JavaScript не работает
- Откройте консоль браузера (F12) и проверьте ошибки
- Убедитесь, что все пути к JS файлам относительные
- Проверьте, что файлы загружены в правильные папки

### database.json не загружается
- Проверьте, что файл `database.json` находится в корне репозитория
- Убедитесь, что файл имеет правильный JSON формат
- Проверьте консоль браузера на наличие ошибок CORS (не должно быть)

## 📝 Чек-лист перед деплоем

- [ ] Все файлы загружены в репозиторий
- [ ] Файл `.nojekyll` присутствует в корне
- [ ] Файл `database.json` присутствует и валиден
- [ ] Все пути к ресурсам относительные (без `/` в начале)
- [ ] GitHub Pages настроен на ветку `main` и папку `/ (root)`
- [ ] Сайт доступен по ссылке GitHub Pages
- [ ] Проверена работа основных функций (вход, регистрация, бронирование)

---

**Готово!** Ваш сайт должен быть доступен по адресу: `https://your-username.github.io/Eco-Motor_Project/`

